# Prospective ledger

Phase A leaves this ledger inactive and empty. After independent audit and protocol tagging, `activation.py` creates the immutable activation record. Prediction snapshots, outcome events, and audit events then remain append-only and hash chained. Never place historical fixtures here.
